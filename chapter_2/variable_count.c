// 声明变量并在插入变量值时进行运算
#include <stdio.h>

int main(void)
{
    int toes;
    toes = 10;

    printf("toes is %d, toes double is %d, toes square is %d.", 
            toes, toes * 2, toes * toes);

    return 0;
}